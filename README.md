# easy-calculator

### I still learning so please be understanding.

## Setup

To run this project, install it locally using npm:

```
$ npm install
```

To run tests watching:

```
$ npm run test:watch
```
